# Silens Clamor - Backend

Backend to bring track info from LastFM

```
docker build --build-arg ARG_LASTFM_APIKEY=${LASTFM_APIKEY} --build-arg ARG_LASTFM_URL=${LASTFM_URL} azratul/silens-clamor/backend:1.0.0 .
```
